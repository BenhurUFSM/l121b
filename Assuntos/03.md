```c
#include <stdio.h>

int main()
{
  int res;
  int v2;
  printf("Digite um numero ");
  scanf("%d", &v2);
  printf("voce digitou %d.\n", v2);
  res = v2*v2;
  printf("o quadrado é %d.\n", res);
  return 0;
}
```
